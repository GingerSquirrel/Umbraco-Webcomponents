export interface CallToActionArgs {
  // Empty interface since we're not using arguments
}

export const Template = () => {
  return `<call-to-action-component></call-to-action-component>`;
};

export const defaultArgs: CallToActionArgs = {
  // No default args needed
};

export const argTypes = {
  // No argTypes needed for argument-less stories
};
